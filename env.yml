name: AgenticAI

dependencies:
  - python >= 3.12
  - pip:
      - phidata
      - python-dotenv
      - yfinance
      - packaging
      - duckduckgo-search
      - fastapi
      - uvicorn
      - groq
      - openai
